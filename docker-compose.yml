services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: itpassport
      POSTGRES_USER: itpassport
      POSTGRES_PASSWORD: itpassport
    ports:
      - "5432:5432"
    volumes:
      - itpassport-pgdata:/var/lib/postgresql/data

volumes:
  itpassport-pgdata:
